query FetchProductDetails($productId: String!) {
  product(id: $productId) {
    id
    key
    masterData { staged { ...Product } }
  }
}

fragment Product on ProductData {
  skus,
  nameAllLocales {
    value, locale
  },
  variants {
    images {
      url
    }
  }
}
